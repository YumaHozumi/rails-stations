<table>
    <tr>
        <th></th>
        <th>
    </tr>
<% @row_sheets.each do |row, sheets| %>
    <tr>
        <th><%= row %></th>
        <% sheets.each do |sheet| %>
            <td><%= sheet.column %></td>
        <% end %>
    </tr>
</table>
